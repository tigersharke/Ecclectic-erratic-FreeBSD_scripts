#!/bin/sh
cd ~
rm ~/thefiles ~/finalfilelist
pkg origin `echo "n" | pkg remove $1 | awk '/\t/'| sed "s:^.::1" `|tee -a thefiles 
tail -n +2 thefiles >> finalfilelist
#yes|pkg remove $1
#echo "---"
#echo "now sending to synth"
#yes|synth force `cat ~/finalfilelist`
